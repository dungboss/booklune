{% if url != blank %}
  <a href="{{ url }}">{{ label | escape }}</a>
{% else %}
  <button type="button" class="pm-text-button pm-footer-help-link" data-pm-open="{{ dialog_id }}">{{ label | escape }}</button>
{% endif %}
